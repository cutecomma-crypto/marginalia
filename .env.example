# Marginalia — Supabase 設定說明
#
# Marginalia 是零建置的純靜態站（GitHub Pages 直接放檔案，沒有打包工具／伺服器
# 能在部署時讀取 .env、把值注入程式碼），所以這個檔案本身「不會」被程式讀取，
# 純粹是文件用途，告訴你要去哪裡拿這兩個值、實際上要填在哪裡。
#
# 真正生效的地方是 js/config.js（會被 commit 進 git）。這是 Supabase 官方
# 對純前端 App 的建議做法：SUPABASE_ANON_KEY 本來就設計成可以公開內嵌在前端，
# 資料安全防線是資料庫端的 Row Level Security（見 supabase/schema.sql），
# 不是靠隱藏這把 key。

# 到 https://supabase.com 建立專案後，在 Project Settings → API 頁面可以找到：

# 專案的 API URL，格式類似 https://xxxxxxxxxxxx.supabase.co
SUPABASE_URL=

# 專案的 anon / public key（不是 service_role key，那把不可以放進前端）
SUPABASE_ANON_KEY=

# ---- 設定步驟 ----
# 1. 到 supabase.com 建立一個新專案（免費方案即可）
# 2. Project Settings → API，複製 Project URL 跟 anon public key
# 3. 把 supabase/schema.sql 整份貼到 Supabase 後台的 SQL Editor 執行一次
# 4. 把上面複製的兩個值填進 js/config.js 對應的 SUPABASE_URL / SUPABASE_ANON_KEY
# 5.（建議）Authentication → Email Templates 確認註冊驗證信模板
