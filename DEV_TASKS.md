# Marginalia｜P0 開發任務拆解
你永遠都必須說繁體中文
> 依據 `PROJECT_SPEC.md` 第 12 節 P0 範圍與第 16 節技術選型（純 HTML/CSS/JS + 圖譜函式庫，無後端）拆解。
> 排序依「資料層 → 基本 CRUD → 各功能模組 → 圖譜 → 統計／備份 → 首頁整合」的依賴關係，非絕對必須照順序做，但前面沒做完，後面會卡住。

---

## 開始前的一個技術決定

「本地 JSON 檔案」在純網頁（無後端）架構下，實際上分兩層：

- **即時儲存**：瀏覽器不能自由讀寫任意本地 JSON 檔，日常操作（新增書、寫筆記）的即時儲存要用瀏覽器內建儲存機制。建議用 **IndexedDB**（容量大、支援結構化資料），不用 localStorage（容量小，5-10MB 上限，資料量大了會不夠用）。
- **JSON 檔案**：對應 SPEC 第 9 節「匯出／匯入／備份」——JSON 是備份與搬家用的格式，不是日常讀寫的地方。

這個區分會影響任務 1 的實作方式，先說明清楚，之後開始寫程式時不用重新討論。

---

## 任務清單

### 1. 專案骨架與資料層
- 建立資料夾結構（`index.html`、`css/`、`js/`）
- 引入圖譜函式庫（D3.js 或 Cytoscape.js，實作時依圖譜互動需求細選）
- 設計 IndexedDB 資料結構：books、reading_records、outputs、notes、nodes、edges 幾個 object store
- 實作最基本的存取層（新增／讀取／更新／刪除的共用函式）

### 2. 書籍資料庫 CRUD
- 新增／編輯書籍表單（SPEC 第 1 節全部欄位：基本資料、擁有／購買資料、兩層分類）
- 書籍列表頁
- 書籍詳細頁
- 刪除書籍（含確認）

> 里程碑：做完 1-2，已經有一個可以「登記一本書」的最小工具。

### 3. 閱讀狀態與紀錄
- 狀態切換（想讀／閱讀中／已讀完／暫停／棄讀／重讀）
- 進度輸入（第 X／Y 頁，自動算百分比）、開始／完成日期、閱讀次數
- 評分

### 4. 閱讀輸出
- 閱讀動機（選項＋自由輸入）
- 閱讀後輸出（多選標籤＋自由輸入，全部選填）

### 5. 快速筆記
- 低摩擦輸入框（不要求分類／標籤／關聯）
- 筆記列表檢視

> 里程碑：做完 1-5，已經是一個「可以每天實際拿來記錄閱讀」的堪用版本，可以先開始真實使用、累積資料，圖譜留到之後不影響現在開始用。

### 6. 搜尋
- 跨書名／作者／筆記內容的搜尋

### 7. 本書圖譜
- 節點資料結構（type、label、所屬書籍、自由欄位）
- 關係資料結構（from、to、label——線本身要帶語意，對照 SPEC 5.4）
- 圖譜視覺化渲染（node-link diagram，綁定單一書籍）
- 新增／編輯節點與關係的 UI

### 8. 閱讀統計（自動計算，對照 SPEC 第 3 節與 B 原則 6）
- 依完成日期自動彙總「每年讀完幾本」（可查任何年份）
- 閱讀中本數、各類型書籍數量、每月完成本數、平均評分、最常閱讀類型

### 9. 匯入／匯出／備份
- 匯出全部資料為 JSON
- 匯入 JSON 還原資料（含基本驗證，避免匯入壞檔案清空現有資料）

### 10. 首頁整合
- 我的閱讀（想讀／閱讀中／已讀完／最近閱讀）
- 閱讀統計
- 最近輸出
- 最近關聯

---

## 建議的下一步

任務 1-5 是最小可用核心，做完就能開始真實使用；任務 7（本書圖譜）是整個 P0 裡最複雜的一塊，獨立於 3-6，可以晚一點做而不卡住其他進度。

要不要從**任務 1（專案骨架與資料層）**開始？
